services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: opensheets
      POSTGRES_PASSWORD: opensheets
      POSTGRES_DB: opensheets
    ports:
      - "5432:5432"
    volumes:
      - opensheets_pg:/var/lib/postgresql/data

volumes:
  opensheets_pg:
